name: AUTO SHEDULING BACKUP

on:
 schedule:
   - cron: "28 12 31 * *"

jobs:
 Sheduling:
  runs-on: ubuntu-latest

  steps:
   - name: Auto sheduling
     run: echo "your sheduling has been started so just wait and watch"
     
